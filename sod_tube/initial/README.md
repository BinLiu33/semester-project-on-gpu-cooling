This directory contains three `.tipsy` files as initial conditions.  
Each initial condition has three `.par` files corresponding to three cooling models.  

Change the `.par` file to edit the I/O path
